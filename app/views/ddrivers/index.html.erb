<div class="container">
  <h1>Delivery Drivers</h1>
  <table class="table">
    <thead>
      <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @ddrivers.each do | ddriver | %>
        <% if ddriver.active == true %>
        <tr>
          <td><%= ddriver.id %></td>
          <td><%= ddriver.first_name %></td>
          <td><%= ddriver.last_name %></td>
          <td>
            <%= link_to 'Show', ddriver, class: "btn btn-outline-primary my-1" %>
            <%= link_to 'Edit', edit_ddriver_path(ddriver), class: "btn btn-outline-secondary my-1" %>
            <%= link_to 'Deactive', deactive_ddriver_path(ddriver), data: { turbo_method: "patch", turbo_confirm: "Are you sure?" }, class: "btn btn-outline-danger my-1", remote: true %>
          </td>
        </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
  <%= link_to '+ driver', new_ddriver_path, class: "btn btn-outline-success my-1" %>
  <%= link_to 'Set delivery fees', delivery_fees_path, class: "btn btn-outline-success my-1" %>
</div>