<div class= "container bg-white text-dark mx-auto">
  <div class = "border border-dark p-3 mb-3">
    <h2>PizzaShop Registration new user</h2>
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
      <div class="form-inputs p-3 mb-3">
        <%= f.input :phone_number,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %>
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password" } %>
        <%= f.input :name,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "name" } %>
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email" } %>
        <%= f.input :street_name,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "stree_name" } %>
        <%= f.input :house_number,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "house_number" } %>
        <%= f.input :additional_info,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "additional_info" } %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Log in", class: "btn btn-outline-secondary"%>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>
