<div class="container">
  <% @pizzas.each do | product | %>
    <h3><%= product.name %> </h3>
    <h4><%= product.description %></h4>
    <h4><%= number_to_currency(product.price) %></h4>
    <%= button_to "Add to cart", order_products_path(:product_id => product.id) %>
  <% end%>
</div>
